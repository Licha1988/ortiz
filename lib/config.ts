/** Capacidad máxima de cubiertos simultáneos del local. */
export const LOCAL_CAPACITY = 180;

/** Cargas sociales sobre salario neto (Argentina). */
export const CCSS_RATE = 0.34;

/** Semanas equivalentes por mes para proyecciones. */
export const WEEKS_PER_MONTH = 4.33;
